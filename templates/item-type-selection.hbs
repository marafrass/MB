<div class="item-type-selection">
  <p class="selection-prompt">Select item type:</p>
  
  <div class="item-type-buttons">
    <button class="item-type-btn note-btn" data-type="Note">
      <span class="icon">📝</span>
      <span class="label">{{noteLabel}}</span>
    </button>
    
    <button class="item-type-btn text-btn" data-type="Text">
      <span class="icon">💬</span>
      <span class="label">{{textLabel}}</span>
    </button>
    
    <button class="item-type-btn image-btn" data-type="Image">
      <span class="icon">🖼️</span>
      <span class="label">{{imageLabel}}</span>
    </button>
    
    <button class="item-type-btn document-btn" data-type="Document">
      <span class="icon">📄</span>
      <span class="label">{{documentLabel}}</span>
    </button>
  </div>
</div>
